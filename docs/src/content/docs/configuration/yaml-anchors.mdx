---
title: YAML Anchors
description: Using YAML anchors for reusable configuration blocks.
sidebar:
  order: 4
---

Use YAML anchors for reusable blocks alongside templates:

```yaml
anchor_bootstrap: &bootstrap_defaults
  disk_encryption_keys:
    - local_path: /tmp/disko-encryption-password.txt
      remote_path: /tmp/disko-encryption-password.txt

fleet:
  flakes:
    infrastructure:
      installables:
        nixosConfigurations:
          server:
            machines:
              server-01:
                bootstrap:
                  <<: *bootstrap_defaults
                  post_bootstrap_hooks:
                    - systemd-cryptenroll --tpm2-device=auto /dev/sda2
```

:::caution
When specifying YAML anchor keys, you must prefix them with `anchor_` for them not to be rejected by the parser.
:::
