---
title: Snapshots
description: Capture and replay deployment workflow state.
sidebar:
  order: 8
---

Snapshots capture the full state of a deployment workflow at a point in time and save it to a JSON file. They include machine states, phase results, command outputs, inspect metadata, and timing information.

**Why snapshots?** When a deployment fails, a snapshot preserves the exact state for later inspection, debugging, sharing with others or to give them as context to LLMs. Instead of scrolling through TUI logs or parsing terminal scrollback, you get a structured record of everything that happened.

:::tip[Use snapshots as LLM context]
A snapshot is a self-contained JSON record of an entire deployment run, including machine states, phase results, command outputs, inspect metadata, and timing. When a deployment fails, paste the snapshot into an LLM prompt instead of scrolling through TUI logs or terminal scrollback.

See real snapshot examples in the [`examples`](https://github.com/mihakrumpestar/panix/tree/main/examples) directory.

Here's what a snapshot contains (trimmed for brevity):

```json
{
  "nix": "nix (Nix) 2.35.1",
  "snapshot": {
    "panix_version": "0.9.1",
    "start_time": "2026-08-15T16:46:07.799909311+02:00",
    "reason": "manual",
    "snapshot_time": "2026-08-15T16:47:10.796833231+02:00"
  },
  "phases": ["inspect", "build", "transfer", "activate"],
  "flags": {
    "config": "examples/panix.deploy.yml",
    "tags": ["personal-workstation"],
    "timeout": 5500000000000,
    "output": "tui"
  },
  "fleet": {
    "flakes": [
      {
        "key": "infrastructure",
        "value": {
          "url": "../infrastructure",
          "installables": [
            {
              "key": "nixosConfigurations",
              "value": [
                {
                  "key": "personal-workstation",
                  "value": {
                    "machines": [
                      {
                        "key": "personal-workstation",
                        "value": {
                          "ssh": { "hostname": "10.0.200.48", "port": 22222, "username": "root" },
                          "meta_inspect": {
                            "reachable": true,
                            "is_root": true,
                            "bootstrapped": true,
                            "architecture": "x86_64",
                            "generations": { "current": 205 },
                            "nixos": "26.11 (Zokor)",
                            "kernel": "7.1.5"
                          },
                          "logs": {
                            "phase_logs": [
                              {
                                "key": "inspect",
                                "value": {
                                  "command_logs": [
                                    {
                                      "description": "bootstrap detection",
                                      "command": "ssh ... personal-workstation cat /etc/os-release",
                                      "output": ["ID=nixos", "PRETTY_NAME=\"NixOS 26.11 (Zokor)\"", "VERSION=\"26.11 (Zokor)\""],
                                      "time_and_state": { "duration": 11652245 }
                                    }
                                  ]
                                }
                              },
                              {
                                "key": "activate",
                                "value": {
                                  "command_logs": [
                                    {
                                      "description": "activate",
                                      "command": "ssh ... personal-workstation /nix/store/.../bin/switch-to-configuration switch",
                                      "output": [
                                        "activating the configuration...",
                                        "setting up /etc...",
                                        "restarting the following user units: nixos-activation.service",
                                        "the following new units were started: NetworkManager-dispatcher.service"
                                      ],
                                      "time_and_state": { "duration": 7738726666 }
                                    }
                                  ]
                                }
                              }
                            ]
                          },
                          "machine_state": { "status": "done", "phase": "activate", "active_ssh": "regular" }
                        }
                      },
                      {
                        "key": "fake",
                        "value": {
                          "ssh": { "hostname": "fake", "port": 22, "username": "root" },
                          "meta_inspect": {},
                          "logs": {
                            "phase_logs": [
                              {
                                "key": "inspect",
                                "value": {
                                  "command_logs": [
                                    {
                                      "description": "SSH reachability check",
                                      "command": "",
                                      "output": [],
                                      "time_and_state": { "duration": 66892226, "end_error": "regular SSH is unreachable" }
                                    }
                                  ]
                                }
                              }
                            ]
                          },
                          "machine_state": {
                            "status": "failed",
                            "status_msg": "SSH unreachable",
                            "phase": "inspect",
                            "error": "reachability check failed: regular SSH is unreachable"
                          }
                        }
                      }
                    ],
                    "meta_build": {
                      "closure": "/nix/store/bh3m91dqz2390fwgmvda408f2dnl8p0s-nixos-system-personal-workstation-26.11.20260802.6438090"
                    },
                    "logs": {
                      "phase_logs": [
                        {
                          "key": "build",
                          "value": {
                            "command_logs": [
                              {
                                "description": "build system closure",
                                "command": "nix ... build --no-link --print-out-paths ../infrastructure#nixosConfigurations.personal-workstation.config.system.build.toplevel",
                                "output": ["/nix/store/...-nixos-system-personal-workstation-..."],
                                "time_and_state": { "duration": 51724055792 }
                              }
                            ]
                          }
                        }
                      ]
                    }
                  }
                }
              ]
            }
          ]
        }
      }
    ]
  }
}
```
:::

## Taking Snapshots

Three ways to take a snapshot:

| Method | Description |
|--------|-------------|
| Press `s` in TUI | Manual snapshot at any time |
| `--snapshot.on-retry` | Automatic snapshot before retrying failed phases |
| `--snapshot.on-exit` | Automatic snapshot on exit |

Snapshot files are written to `--snapshot.dir` (default: current directory) with the naming format:

```text
panix-snapshot-<start_epoch>-<snapshot_epoch>-<reason>.json
```

Where `<reason>` is `manual`, `retry`, or `exit`.

## Viewing Snapshots

Replay any snapshot in the TUI:

```sh
panix snapshot --path panix-snapshot-1776379281-1776379290-manual.json
```

This opens the familiar TUI view with all phase statuses, build logs, command outputs, and machine stats frozen at the time the snapshot was taken. Phases and commands that were running at the time of capture will also appear as running (loading spinners) in TUI replay.

:::note
`r` (retry) and `ctrl+r` (restart) keybinds are disabled in snapshot view since the workflow is not running.
:::

## Configuration

```yaml
flags:
  snapshot:
    dir: .                    # Directory to save snapshots
    on_retry: true            # Take snapshot before retry
    on_exit: true             # Take snapshot on exit
```

Or via CLI flags:

```sh
panix deploy --snapshot.dir=./snapshots --snapshot.on-retry --snapshot.on-exit
```
