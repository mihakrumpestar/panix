---
title: Installation
description: Run Panix directly with nix or add it to your flake.
sidebar:
  order: 2
---

## 1. Run directly

```sh
nix run github:mihakrumpestar/panix -- deploy
```

## 2. Add to your flake

Add Panix as a flake input and include it in your system packages:

```nix
{
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";

    panix.url = "github:mihakrumpestar/panix";
  };

  outputs = { self, nixpkgs, panix, ... }@inputs:
  let
    system = "x86_64-linux";
  in {
    nixosConfigurations.my-server = nixpkgs.lib.nixosSystem {
      inherit system;
      modules = [
        ./configuration.nix
        {
          environment.systemPackages = [
            panix.packages.${system}.default
          ];
        }
      ];
    };
  };
}
```

:::note[Binary cache]
CI pushes build results to a Cachix cache, so prebuilt binaries are fetched instead of compiling from source (compiling takes about 10s on mid-range hardware). To opt in, add the cache to your flake:

```nix
{
  nixConfig = {
    extra-substituters = [ "https://mihakrumpestar.cachix.org" ];
    extra-trusted-public-keys = [ "mihakrumpestar.cachix.org-1:7mKgpsk+9+iJcaT5DwvUtMxlJS+1GH/dlYhzoFkefJg=" ];
  };
}
```
:::
