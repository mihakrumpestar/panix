# yaml-language-server: $schema=./gen/panix-schema.yaml

flags:
  timeout: 5500s
  override_local_machine: tmp

root:
  flakes:
    infrastructure:
      url: path:../infrastructure
      configurations:
        #server-03:
        #  machines:
        #    server-03:
        #      tags:
        #        - test
        #    docker-swarm:
        personal-workstation:
          machines:
            personal-workstation:
              #secrets:
              #  - local_path: ./README.md
              #    remote_path: /tmp/README2.md
            fake:
              ssh:
                hostname: fake
                username: root
                port: 22
              bootstrap:
                disk_encryption_keys:
                  - local_path: /home/user/secrets/luks.key
                    remote_path: /tmp/luks-key

        personal-laptop:
          machines:
            personal-laptop:
        server-01:
          machines:
            server-01:
        vps-02:
          machines:
            vps-02:
        kiosk:
          machines:
            kiosk:
              ssh:
                hostname: 10.0.200.212
                #hostname: 10.0.60.2
                username: root
                port: 22222
                #identity_file: temp_key
              #bootstrap:
              #  post_bootstrap_hook: systemd-cryptenroll --tpm2-device=auto --tpm2-with-pin=no /dev/mmcblk1p2