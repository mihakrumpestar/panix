flags:
  #bootstrap:
  #  autoBootstrap: false
  timeout: 7200 # 2h
  requireAllSuccess: false
  overrideLocalMachine: tmp
  #dryRun: false

root:
  flakes:
    infrastructure:
      url: path:../infrastructure
      flakeHooks:
        pre: exit 0
        post: exit 0
      configurations:
        #server-03:
        #  machines:
        #    server-03:
        #      tags:
        #        - test
        #    docker-swarm:
        personal-workstation:
          machines:
            personal-workstation: 
              #secrets:
              #  - local:
              #      path: ./README.md
              #    remote:
              #      path: /tmp/README.md
              #  - local:
              #      commandOutput: echo OK
              #    remote:
              #      path: /tmp/README2.md
            fake:
              ssh:
                hostname: fake
                username: root
                port: 22
        personal-laptop:
          machines:
            personal-laptop:
        server-01:
          machines:
            server-01:
        kiosk:
          machines:
            kiosk:
              ssh:
                hostname: 10.0.200.212
                #hostname: 10.0.60.2
                username: root
                port: 22222
                #identityFile: temp_key
              #postBootstrapHook: systemd-cryptenroll --tpm2-device=auto --tpm2-with-pin=no /dev/mmcblk1p2