global:
  #bootstrap:
  #  autoBootstrap: false
  concurrency: 0
  timeout: 7200 # 2h
  requireAllSuccess: false
  #dryRun: false

root:
  flakes:
    infrastructure:
      url: path:../infrastructure
      flakeHooks:
        pre: exit 0
      configurations:
        server-03:
          machines:
            server-03:
              tags:
                - test
        #    docker-swarm:
        personal-workstation:
          machines:
            personal-workstation: 
              secrets:
                - local:
                    path: ./README.md
                  remote:
                    path: /tmp/README.md
                - local:
                    commandOutput: echo OK
                  remote:
                    path: /tmp/README2.md
            fake:
