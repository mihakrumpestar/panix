# yaml-language-server: $schema=./panix-schema.yaml

flags:
  #bootstrap:
  #  auto_bootstrap: false
  timeout: 5500s
  override_local_machine: tmp
  #dry_run: false

root:
  flakes:
    infrastructure:
      url: path:../infrastructure
      flake_hooks:
        pre: exit 0
        post: exit 0
      configurations:
        #server-03:
        #  machines:
        #    server-03:
        #      tags:
        #        - test
        #    docker-swarm:
        personal-workstation:
          machines:
            personal-workstation:
              #secrets:
              #  - local:
              #      path: ./README.md
              #    remote:
              #      path: /tmp/README.md
              #  - local:
              #      command_output: echo OK
              #    remote:
              #      path: /tmp/README2.md
            fake:
              ssh:
                hostname: fake
                username: root
                port: 22
              bootstrap:
                disk_encryption_keys:
                  - local: /home/user/secrets/luks.key
                    remote: /tmp/luks-key

        personal-laptop:
          machines:
            personal-laptop:
        server-01:
          machines:
            server-01:
        kiosk:
          machines:
            kiosk:
              ssh:
                hostname: 10.0.200.212
                #hostname: 10.0.60.2
                username: root
                port: 22222
                #identity_file: temp_key
              #bootstrap:
              #  post_bootstrap_hook: systemd-cryptenroll --tpm2-device=auto --tpm2-with-pin=no /dev/mmcblk1p2