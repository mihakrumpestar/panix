flags:
  #bootstrap:
  #  autoBootstrap: false
  timeout: 7200 # 2h
  requireAllSuccess: false
  #dryRun: false

root:
  flakes:
    infrastructure:
      url: path:../infrastructure
      flakeHooks:
        pre: exit 0
        post: exit 0
      configurations:
        #server-03:
        #  machines:
        #    server-03:
        #      tags:
        #        - test
        #    docker-swarm:
        personal-workstation:
          machines:
            personal-workstation: 
              #secrets:
              #  - local:
              #      path: ./README.md
              #    remote:
              #      path: /tmp/README.md
              #  - local:
              #      commandOutput: echo OK
              #    remote:
              #      path: /tmp/README2.md
            fake:
              ssh:
                hostname: fake
                username: root
                port: 22
        personal-laptop:
          machines:
            personal-laptop:
        kiosk:
          machines:
            kiosk:
              ssh:
                hostname: 10.0.200.74
                port: 22222
                username: root
